# LSSB — Lagos State Scholarship Board

Scholarship application portal. Repo `LSSB`.

## ⚠️ Frontend is a COMPILED bundle (React source lost)
The original React source is gone — the deployed frontend is a **compiled bundle**.
Make frontend changes by **patching the built bundle** (bundle-patching workflow),
NOT by editing React source. (One historical bug was unresolvable purely because the
source is lost.)

Detailed memory: this repo's Claude namespace `MEMORY.md` (`project-lssb`).
